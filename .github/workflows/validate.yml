name: Validate skillset

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Local package validation
        run: bash tests/validate-package.sh
      - name: Optional link validation
        run: bash tests/validate-links.sh || true
